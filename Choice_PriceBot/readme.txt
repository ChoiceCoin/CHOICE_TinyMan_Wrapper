This bot relies on API keys in an untracked file: keys.json
If necessary, contact a repo contributor for the keys
The keys file can also be recreated with a new discord API key and PureStake Algod API key